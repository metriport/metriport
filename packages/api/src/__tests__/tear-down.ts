/**
 * From Jest: This option allows the use of a custom global teardown module which must export a
 * function (it can be sync or async). The function will be triggered once after all test suites
 * and it will receive two arguments: Jest's globalConfig and projectConfig.
 * @see https://jestjs.io/docs/configuration#globalteardown-string
 */
// eslint-disable-next-line @typescript-eslint/no-empty-function
module.exports = async function () {};
