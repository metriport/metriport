{
    "display": 
        {{#if (eq code "social-history")}}
            "Social History"
        {{else if (eq code "vital-signs")}}
            "Vital Signs"
        {{else if (eq code "laboratory")}}
            "Laboratory"
        {{else if (eq code "procedure")}}
            "Procedure"
        {{/if}}
}

