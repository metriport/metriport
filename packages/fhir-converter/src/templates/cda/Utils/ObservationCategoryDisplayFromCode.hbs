{
    "display": 
        {{#if (eq code "social-history")}}
            {{log "in social-history"}}
            "Social History"
        {{else if (eq code "vital-signs")}}
            {{log "in vital-signs"}}
            "Vital Signs"
        {{else if (eq code "laboratory")}}
            {{log "in laboratory"}}
            "Laboratory"
        {{else if (eq code "procedure")}}
            {{log "in procedure"}}
            "Procedure"
        {{/if}}
}

