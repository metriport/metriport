---
title: "Get Document URL"
description: "Gets a presigned URL for downloading the specified document."
api: "GET /medical/v1/document/downloadUrl"
---

This endpoint returns a URL which you can use to download the specified document using the file name provided from the [List Documents endpoint](/medical-api/api-reference/document/list-documents)

## Query Params

<ParamField query="fileName" type="string" required>
  The file name of the document.
</ParamField>

## Response

<ResponseField type="string">The presigned URL.</ResponseField>

<ResponseExample>

```javascript Metriport SDK
import { MetriportMedicalApi } from "@metriport/api";

const metriportClient = new MetriportMedicalApi("YOUR_API_KEY");

const presignedUrl = await metriportClient.getDocumentUrl("abc123-def456");
```

</ResponseExample>
