  <Expandable title="properties">
    <ResponseField name="addressLine1" type="string">
      The address.
    </ResponseField>

    <ResponseField name="addressLine2" type="string">
      The address details, for example: `#4451`.
    </ResponseField>

    <ResponseField name="city" type="string">
      The city.
    </ResponseField>

    <ResponseField name="state" type="string">
      The 2 letter state acronym, for example: `CA`.
    </ResponseField>

    <ResponseField name="zip" type="string" required>
      Zip must be either 5 digits (e.g., 12345) or 9 digits with hyphen (ZIP+4 format, e.g., 12345-1234)
    </ResponseField>

    <ResponseField name="country" type="string" required>
      Always "USA".
    </ResponseField>

  </Expandable>
