<ResponseField name="oid" type="string" required>
  The OID of this network.
</ResponseField>

<ResponseField name="name" type="string">
  The name of this network.
</ResponseField>

<ResponseField name="zip" type="string">
  The zipcode this network resides in.
</ResponseField>

<ResponseField name="state" type="string[]">
  The two letter statecode for the state this network resides in. For example, `CA`, `NY`, or `TX`.
</ResponseField>

<ResponseField name="rootOrganization" type="string">
  The root organization.
</ResponseField>

<ResponseField name="managingOrgId" type="string">
  The ID of the organization that is the direct parent.
</ResponseField>