name: PR Check - Infra

on:
  pull_request:
    paths:
      - "packages/infra/**"
  merge_group:
  workflow_dispatch:

jobs:
  check-pr:
    uses: ./.github/workflows/_build.yml
    with:
      path: "packages/infra"
    secrets: inherit
