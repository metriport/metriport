{{#if (eq code 'HP')}}
	"home"
{{else if (eq code 'H')}}
	"home"
{{else if (eq code 'WP')}}
	"work"
{{else if (eq code 'TMP')}}
	"temp"
{{else if (eq code 'BAD')}}
	"old"
{{else if (eq code 'MC')}}
	"mobile"
{{else}}
	"temp"
{{/if}}