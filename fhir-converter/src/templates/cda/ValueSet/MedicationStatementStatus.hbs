{{#if code}}
  {{#if (eq (toLower code) 'aborted')}}
    "stopped"
  {{else if (eq (toLower code) 'ordered')}}
    "active"
  {{else if (eq (toLower code) 'suspended')}}
    "on-hold"
  {{else}}
    "{{code}}"
  {{/if}}
{{/if}}