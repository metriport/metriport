description: Invalid request. The request body is missing required fields or contains invalid data.
content:
  application/json:
    schema:
      type: object
      properties:
        status:
          type: integer
          enum: [400]
        title:
          type: string
        detail:
          type: string
        name:
          type: string
    example:
      status: 400
      title: "BadRequestError"
      detail: "[Information]"
      name: "Bad Request"