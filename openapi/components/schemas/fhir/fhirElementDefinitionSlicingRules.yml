title: fhirElementDefinitionSlicingRules
type: string
enum:
  - closed
  - open
  - openAtEnd
description: >-
  Whether additional slices are allowed or not. When the slices are ordered,
  profile authors can also say that additional slices are only allowed at the
  end.
