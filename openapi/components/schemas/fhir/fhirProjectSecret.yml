title: fhirProjectSecret
type: object
description: Secure environment variable that can be used to store secrets for bots.
properties:
  name:
    type: string
    description: The secret name.
  valueString:
    type: string
    nullable: true
    description: The secret value.
  valueBoolean:
    type: boolean
    nullable: true
    description: The secret value.
  valueDecimal:
    $ref: ./fhirdecimal.yml
    nullable: true
    description: The secret value.
  valueInteger:
    type: integer
    nullable: true
    description: The secret value.
required:
  - name
